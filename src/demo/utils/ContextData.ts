import { Context } from "./../../Context";
import { AsyncWatch } from "async-watch";
import { Compile } from "wires-angular-expressions";
export class WatchString {
    public watchCompiled(context: Context, data: any[], changed: { (...args) }) {

    }
}
